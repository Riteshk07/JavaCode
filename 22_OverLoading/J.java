class A {
    static void pro(double e) {

    }

    //parameter mismatch: a legal case of method overloading, even though one of them static and the other is non static 

    void pro(float x) {
        
    }
}